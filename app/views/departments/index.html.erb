<h1>部门列表</h1>

<table>
  <thead>
    <tr>
      <th>编号</th>
      <th>名称</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr>
        <td><%= department.serial_number %></td>
        <td><%= department.name %></td>
        <td><%= link_to '查看', department %></td>
        <td><%= link_to '编辑', edit_department_path(department) %></td>
        <td><%= link_to '删除', department, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新建部门', new_department_path %>
