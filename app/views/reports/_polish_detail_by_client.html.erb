<div class="report">
  <table>
    <thead>
      <tr class="title">
        <th colspan="10">打磨统计表（客户）</th>
      </tr>
    </thead>
    <tbody>
    <% @report.each do |report| %>
        <% if report[:type] %>
            <tr class="<%= report[:type] %>">
        <% else %>
            <tr>
        <% end %>
        <td ><%=report[:date] %></td>
        <td><%= report[:client] %></td>
        <td><%= report[:product_name] %></td>
        <td><%= report[:order_number] %></td>
        <td><%= report[:rev_weight] %></td>
        <td><%= report[:dis_weight] %></td>
        <td><%= report[:count] %></td>
        <td><%= report[:user] %></td>
        <td><%= report[:participant] %></td>
        <td><%= report[:employee_name] %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>
