<% if is_admin_permission? session[:permission] %>
<%= render partial: 'filter_date_range', locals: { path: reports_polish_summary_path } %>
<% else %>
    <%= render partial: 'filter_date_range', locals: { path: reports_polish_summary_path } %>
<% end %>
<%= render 'polish_summary' %>
